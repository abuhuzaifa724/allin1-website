[functions]
  node_bundler = "esbuild"

[build]
  functions = "netlify/functions"
```

**"Commit changes"** করো ✅

---

তারপর **২ মিনিট** অপেক্ষা করো এবং আবার এই URL চেক করো 👇
```
allin1news.netlify.app/.netlify/functions/news?category=general
